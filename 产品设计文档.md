# 腕控 WristDeck —— 产品设计文档

> 目标：用 OPPO Watch 3（国行 OWW212 / ColorOS Watch 5.0 / Android 11）远程控制 PC 浏览器。**主场景是抖音网页视频**，但方向键是通用能力，任意网页都可用。
> 状态：**已实现（M1–M3）**，M4 稳定性项部分待办。
> 版本：v0.3.4 ｜ 首次日期：2026-09-07 ｜ 本次修订：2026-09-14
>
> v0.3.4 修订要点：**Bridge 把手表操作日志实时打到终端**。此前 `pushLog()` 只写内存缓冲（供状态页与 `/api/log`），从不输出到 stdout，且**成功转发的指令不记日志**——`node server.js` 跑起来只看得到启动横幅，手表按了什么完全看不见，而"看得见发生了什么"是这套东西最基本的可用性前提。现在终端实时输出「时间 / 来源 / 动作 / 耗时 / 结果 / 备注」，一次按键对应 `手表 … √ sent` + `扩展 … 结果` 两行，失败现场一并带上；stdout 非 TTY 时自动去色，`--quiet` 可关。详见 `技术架构文档.md` §0 / §7.1。
>
> v0.3.3 修订要点：**方向键在普通网页上"按了没反应"的根因修掉了**。v0.3.2 已经解除站点限制并补了原生滚动兜底，但兜底对三类真实页面从未生效——滚动宿主藏在 `overflow:hidden` 的内层容器里（抖音、YouTube、多数后台系统都是这样）、内容在 shadow DOM 里、页面只横向溢出。于是"任意网页可用"实际上仍是空话，现场表现为恒定 `no_change`。v0.3.3 按方向查找真正的滚动宿主并覆盖这三类情况，同时取消"页面里有 `<video>` 就不兜底"这条会误杀内嵌视频文章页的限制。失败也不再是黑盒：`no_change` 会带上现场原因。详见 `技术架构文档.md` §0 / §7.3。
>
> v0.3.2 修订要点：**方向键与播放/暂停解除站点限制，任意网页可用**。此前扩展的注入范围只覆盖 `douyin.com`，且内容脚本"页面没有 `video` 就返回 `no_video`"，两条一起把方向键锁死在抖音——这是把站点特化逻辑错当成通用能力。现在注入范围扩到 `http/https` 全站；普通网页上合成键盘事件不触发浏览器默认滚动，扩展补一次原生滚动兜底。仅 `like` / `fullscreen` 这类抖音快捷键仍限抖音域。详见 `技术架构文档.md` §0 / §7.2 / §7.3。
>
> v0.3 修订要点：主界面由"播放键 + 上一个/下一个"三键改为**十字方向盘 + 播放键居中**；四个方向键**原样转发** `ArrowUp`/`ArrowDown`/`ArrowLeft`/`ArrowRight`，由页面自己解释（抖音里 ↑↓ 翻视频、←→ 快进退 5s，见 §9 已核实项）；协议里 `prev`/`next` 删除。详见 `技术架构文档.md` §0。
>
> v0.2 修订要点（与实现对齐）：主按钮改为发**显式** `play`/`pause`（幂等，重试不会翻转）；指令超时显示"未确认"并有迟到纠正；连点过快会被服务端拒绝；息屏验收标准补上前置条件；状态机去掉实现中不存在的 `AUTHENTICATING` 阶段。详见 `技术架构文档.md` §0。

---

## 1. 背景与结论

### 1.1 蓝牙 HID 路线已否决

原始设想是"手表作为 Bluetooth HID 外设 → 配对 PC → 发送媒体键"。经 adb 实测确认此路不通：

- `com.android.bluetooth/.hid.HidDeviceService` 未运行（`No services match`）；
- 系统 APK 中虽含 HID 相关类，但 `hid device` Profile 未在当前系统配置中启用；
- 第三方应用无法通过 Manifest 声明或 `BluetoothHidDevice` API 强行拉起该 Profile（属于系统级配置，需 ROM 侧 `config_hid_profile_supported` + 服务白名单）。

**结论：不做 HID。** 后续若 OTA 后出现新固件启动该服务，可再评估标准 HID 媒体控制器（本设计的控制端协议可保持不变，仅替换"执行器"）。

### 1.2 选定路线：局域网 WebSocket 中枢

采用"手表 App → 局域网 WebSocket → PC 本地服务（Bridge）→ 浏览器插件 → 当前网页"的方案。

**为什么中间必须有一个 PC 本地服务（Bridge）？**
浏览器扩展（MV3）不能对外开监听端口，只能主动向外连接；而手表需要一个稳定的被连接方。因此 Bridge 作为常驻在本机的 WebSocket 服务端，同时接纳手表和浏览器扩展两个客户端，并负责转发、鉴权、状态回传。

### 1.3 四条执行路线对比

| 路线 | 做法 | 真实按键 | 安装负担 | 结论 |
|---|---|---|---|---|
| A. 浏览器扩展（网页内事件） | content script 操作 `video` 元素 + 合成 `KeyboardEvent` | 否（合成事件） | 低：装扩展 + 跑 Bridge | **MVP 采用** |
| B. CDP 真键 | Chrome 带 `--remote-debugging-port` 启动，Bridge 调 `Input.dispatchKeyEvent` | 是（isTrusted） | 中：需带参数启动浏览器 | P1 兜底开关 |
| C. 手机中转 | 手表 → 手机 App → 局域网 → PC | 否 | 高：多一个 App | 不采用 |
| D. 原生媒体键 | 扩展 + Native Messaging Host 调 OS 级按键 | 是（系统级） | 高：需装 native host，跨平台 | P2 可选 |

路线 A 的弱点（合成事件 `isTrusted=false`）用"直接操作 video 元素 + 结果校验 + 失败自动降级到 B"来补偿。

---

## 2. 目标与非目标

### 2.1 目标

1. 手表上一个十字方向盘（**上/下/左/右**）+ 居中的**播放/暂停**大按钮；方向键原样转发键盘方向键，语义交给页面。
2. 点击后 PC 端抖音网页有实际响应，端到端延迟可感知为"即时"。
3. 手表有明确反馈（震动 + 图标状态），用户不必看 PC 屏幕确认。
4. 断网/重连自动恢复，不需要人去 PC 上点什么。

### 2.2 非目标

- 不做 HID、不做蓝牙配对、不做手机端 App。
- 不做抖音登录态/账号相关能力，不调用任何抖音私有接口。
- 不做视频内容解析、推荐算法干预。
- 不做多人/多 PC 协作（V1 只支持一台 PC）。

---

## 3. 用户场景

### 3.1 主场景：躺沙发刷抖音

1. PC 上开着 Chrome，播放 `https://www.douyin.com` 推荐流。
2. 抬腕，手表 App 在前台（或上次连接自动恢复）。
3. 点方向盘 **▽** → 抖音切到下一条视频；点 **△** → 回上一条。
4. 点 **◁ / ▷** → 视频快退 / 快进 5 秒。
5. 点中央"播放/暂停" → 当前视频暂停，手表震动一下，按钮图标变为 ▶。
6. 息屏 5 分钟后再抬腕点击，仍然一次生效。

### 3.2 首次配置场景（一次性，约 1 分钟）

1. PC 上执行 `cd bridge && npm start`，终端打印本机局域网地址、端口、6 位 PIN。
2. 打开 `http://localhost:8787` 看到状态页（含 PIN、已连接客户端、实时指令日志）。
3. 手表上输入 IP（或用 mDNS 自动发现点选）+ PIN → 保存。
4. Chrome 加载扩展（开发者模式）→ 扩展自动连上 Bridge → 状态页显示"执行器已就绪"。

---

## 4. 功能清单

### P0（MVP，必须）

| ID | 功能 | 说明 |
|---|---|---|
| F1 | 手表方向盘控制 | 十字方向盘（上/下/左/右）+ 居中播放/暂停，方向键原样转发键盘事件，**任意网页可用**，支持连点 |
| F2 | 手表连接管理 | 输入/发现 PC（IP + 端口 + PIN）、连接状态指示、一键重连 |
| F3 | PC Bridge 服务 | WebSocket 中枢 + 本地状态页 + 指令日志 |
| F4 | 浏览器扩展 | 接收指令 → 投给当前活动网页（video API 优先，合成键盘事件兜底；抖音快捷键类动作限抖音域） |
| F5 | 闭环反馈 | 执行结果回传 → 手表震动 + 播放状态回显；失败时不同震动模式 |

### P1（MVP 之后立刻补）

| ID | 功能 | 说明 |
|---|---|---|
| F6 | mDNS 自动发现 | Bridge 广播 `_wristbridge._tcp`，手表自动列出候选 PC，免手输 IP |
| F7 | 后台常连 / 息屏可用 | 前台服务 + 锁，抬腕即用 |
| F8 | 扩展动作扩展 | 音量 +/−、点赞、全屏、快退/快进 5s（复用同一协议） |
| F9 | CDP 真键降级 | 合成事件失败时自动切到 CDP 通道（需在扩展里开启开关） |

### P2（可选）

| ID | 功能 |
|---|---|
| F10 | 表冠旋转翻页（需验证 ColorOS Watch 是否暴露旋转表冠 API） |
| F11 | Native Messaging Host 原生媒体键（可作用于任意焦点窗口，跨平台分实现） |
| F12 | Bridge 开机自启（macOS launchd / Windows 计划任务 / Linux systemd） |
| F13 | 多 PC 配置切换 |

---

## 5. 信息架构与界面设计

### 5.1 手表端（屏幕 372×430，实测 OWW212）

**主界面（单页，纵向三区）**

```
┌──────────────────────────┐
│ ● 客厅PC            已连接 │  ← 状态条：绿点=已连接 / 灰点=断开 / 黄点=重连中
├──────────────────────────┤
│             △            │  ← 上：ArrowUp
│          ◁  ▶  ▷         │  ← 左/右：ArrowLeft / ArrowRight
│             ▽            │     中央播放/暂停最大，▶ / ❚❚ 随状态切换
├──────────────────────────┤
│           设置            │  ← 底部小入口
└──────────────────────────┘
```

**尺寸核算**（`wm size` = 372×430 px、`wm density` = 320 → 可用区仅 **186×215 dp**，纵向必须逐项算，否则底部会被裁掉）

| 区域 | 高度 | 宽度 |
|---|---|---|
| padding 6 + 状态条 | 6 + 约 15 | — |
| △ | 45（表盘宽 0.26） | 45 |
| ◁ / 播放 / ▷ | 57（播放键 0.33，全屏最大） | 45 + 5 + 57 + 5 + 45 = 157 |
| ▽ | 45（表盘宽 0.26） | 45 |
| 设置入口 | 20 | 73（0.42） |
| **合计** | **约 204 dp**（余量 11 dp） | **157 dp ≤ 174 dp** ✓ |

交互细节：
- 按钮按下时立即本地震动 12ms（先给触觉反馈，不等网络回包），收到 ack 后再决定是否需要"失败震动"（长震 60ms）。
- 连点保护：同一按钮 120ms 内去重，避免手抖连发；服务端在途指令超过 3 条会直接拒绝（状态条提示"点太快了"），防止回包乱序。
- **方向键用空心三角、播放键用实心 ▶ / ❚❚**：两者位置相邻，若都用实心三角，"▶ 是播放还是右移"会产生歧义；用填充方式区分比用大小区分更可靠。
- **图标是矢量图，不是字符**：早期版本直接用字符 `△▽◁▷`，实测各字形相对圆心偏移不一致（`◁▷` 低 4px、`▽` 高 1.5px、`△` 居中）—— 不同字形走了不同字体回退。改为 `VectorDrawable`（包围盒在 24×24 视口内严格居中）+ `scaleType="center"`，位置与设备字体无关。三角形描边 1.8/24，与旧字符笔画粗细相当。
- **播放键图标用深色**（`@color/on_accent` = `#0F1115`，压在 `accent` 蓝底上）：对比度 **7.8:1**，而原来的浅色 `@color/text` 只有 **2.0:1**——远低于 4.5:1 的下限，小屏远看会糊成一团。深色值与 `bg` 相同，视觉上等于在蓝色圆盘上"挖"出图标。
- 中央播放键发出的是**显式目标动作**：本地已知在播放就发 `pause`，已知在暂停就发 `play`，状态未知才发 `toggle`。这样失败后重试不会把播放状态翻回去。
- 播放图标先按"目标状态"乐观切换，再由 ack 回传的 `playing` 纠正。超时（1.5s）显示"未确认（可能已执行）"而不是"失败"；若执行器稍后回包，图标会被静默纠正、不重复震动。
- **方向键不做乐观更新**：方向键不改变播放状态，没有可乐观赋值的图标；ack 里的 `playing` 仍会用来纠正播放图标（上下翻页后新视频可能是暂停态）。
- **方向键的失败是真实失败**：扩展会校验页面到底动没动（上下看视频源是否换了、左右看播放进度是否变了、普通网页看滚动位置是否变了），页面没响应就回 `no_change`，手表震失败。按了没反应就应该被告知，不做静默成功。
- **"没反应"要能自证原因**：`no_change` 会带上现场信息（页面里有没有视频、该方向还有没有滚动余量、是不是站点自己吃掉了按键、滚动宿主是文档还是内层容器）。失败却说不清为什么，等于把排查成本全推给用户。

**设置页（纵向滚动）**
- 设备：发现到的 PC 列表 / 手动输入 IP:端口
- PIN：数字输入（首次输入后保存）
- 开关：后台常连、点击震动、手势（预留）
- 操作：测试连接、清除配置、关于（版本号）

### 5.2 PC Bridge 状态页（`http://localhost:8787`）

单页，深色极简，2s 轮询刷新：

- 顶部卡片：配对 PIN、局域网地址（`ws://IP:PORT`）、手表端、浏览器执行器（显示设备名 + 连接时长）
- 统计卡片：指令统计（成功 / 失败，另有"迟到"计数）、指令延迟 P50 / P95（**口径为 Bridge 内部耗时**，标签已注明）
- 底部：最近指令表（时间、来源、动作、耗时、结果、备注），最多 50 条
- 页脚提示：仅在可信局域网使用；**PIN 只在本机（localhost）打开本页时显示**

> 实现中没有"复制连接信息"按钮与"最近心跳"字段；PIN 非本机访问时不返回（技术文档 §10）。

### 5.3 浏览器扩展 Popup

- `Bridge`：连接状态（已连接 / 未连接 + 最近一次错误原因）
- `目标标签`：已锁定的标签页 ID，或"未锁定 · 当前激活"
- `站点规则`：当前标签页 hostname 与生效模式（`feed` 视频流 / `scroll` 通用滚动）
- 两个按钮：**锁定当前标签页** / **重新连接 Bridge**
- 最近执行记录（最多 20 条：动作、耗时、执行方式 `video`/`key`/`feed`、失败原因）

> popup 中"启用 CDP 真键降级""对所有站点生效"两个开关属于 P1，尚未落地。

---

## 6. 状态机与反馈

**手表连接状态机**：`DISCONNECTED → CONNECTING → READY → (LOST) → RECONNECTING(指数退避 1s/2s/4s/8s/15s/30s/60s，上限 60s) → READY`；鉴权失败落 `DENIED`。实现里**没有独立的 `AUTHENTICATING` 阶段**——鉴权结果由 `welcome` / `denied` 直接决定，落在 `READY` / `DENIED`。`DENIED` 中 `bad_pin` / `pin_locked` **不自动重试**（重试无意义，只会烧日志和电量），改 PIN 保存后立即重连。

**指令状态机**：`IDLE → SENT(等待 ack) → OK | UNCONFIRMED(1.5s 未确认) | ERROR`。`UNCONFIRMED` 的指令若在 10s 内收到迟到结果，只做图标纠正，不进入 OK/ERROR 判定。

反馈矩阵：

| 结果 | 手表反馈 |
|---|---|
| OK | 点击时 12ms 短震；播放/暂停图标按回传状态切换 |
| UNCONFIRMED（超时） | 60ms 长震一次，状态条"未确认（可能已执行）"；迟到结果到达后静默纠正图标 |
| 被拒绝（连点过快） | 60ms 长震，状态条"点太快了，稍等" |
| 未连接 | 双短震，状态条灰 |
| 执行器离线 | 60ms 长震，状态条提示"浏览器未就绪" |

> 状态条的"黄色闪烁"在实现中未落地，当前用文案区分状态（见技术文档 §5.2）。

---

## 7. 验收标准（可量化）

| 指标 | 目标值 | 测量方式 |
|---|---|---|
| 端到端延迟（点击→画面变化） | P50 < 80ms，P95 < 150ms | 手表发 `ts`，Bridge 与 content script 各打点，ack 回传分段耗时。**必须在真实链路（手表→Wi-Fi→Bridge→扩展→DOM→回程）上测**；回环 + 模拟执行器得到的"Bridge 内部 P50 ~16ms"不是端到端，不能作为验收依据（技术文档 §9） |
| 指令成功率 | ≥ 99%（同 Wi-Fi，连续 200 次） | 状态页日志统计 |
| 断网恢复 | Wi-Fi 恢复后 < 5s 自动重连 | 手动关 PC Wi-Fi 再开 |
| 息屏可用性 | 息屏 5min 后首次点击成功率 100%。**前置条件：已把 App 加入系统电池白名单** | 息屏计时验证。未加白名单时 ColorOS 会拒绝息屏 wakelock，此项必然不达标（实测见技术文档 §16） |
| 手表耗电 | 连续使用 30min ≤ 3% 电量 | 系统电量读数对比 |
| 首次配置耗时 | ≤ 2 分钟（含装扩展） | 计时 |

---

## 8. 里程碑

| 阶段 | 交付物 | 说明 |
|---|---|---|
| M1 | Bridge + 模拟客户端 | 协议跑通，状态页可看，用脚本模拟手表发指令 |
| M2 | 浏览器扩展 | 在抖音页面上验证"播放/暂停 + 上下切换"真实生效 |
| M3 | 手表 App | adb 安装到 OWW212，端到端联调 |
| M4 | 稳定性 | 重连、息屏、耗电、延迟达标；补 mDNS 发现 |

---

## 9. 风险与未决问题

| 风险 | 影响 | 缓解 |
|---|---|---|
| 抖音前端改版/校验 `isTrusted` | 合成键盘事件失效 | 播放/暂停走 `video` 元素 API（不依赖键盘）；方向键失败则切 CDP 真键（F9） |
| 手表息屏后 Wi-Fi 被系统休眠 | 点击无响应 | 前台服务 + `WifiLock` + `PARTIAL_WAKE_LOCK`；实测确认后再决定是否需要"保持亮屏"开关 |
| ColorOS Watch 后台杀进程 | App 被回收 | 前台服务 + 多任务锁定引导 |

**待用户确认（见文末"需要你拍板"）**

---

## 10. 环境基线（adb 实测，2026-09-07）

| 项 | 实测值 |
|---|---|
| 设备 | `<设备序列号>` |
| 型号 / device | `OWW212` |
| 固件 | `OWW212_11_A.142` |
| Android | 11（API 30） |
| ABI | `armeabi-v7a, armeabi`（**仅 32 位**） |
| Wi-Fi | 已连接，IP `<手表IP>/24` |

> ABI 只有 32 位这条很关键：任何带 native `.so` 的第三方库必须提供 armeabi-v7a，否则装不上或运行崩溃。技术文档中据此做了选型约束。

**已核实（二手来源，待实测复核）**：抖音网页版键盘行为 —— 空格=播放/暂停，↑/↓=上/下一个视频，←/→=快退/快进 5s，H=全屏，Shift+Q=关标签页。M2 阶段用扩展实跑一次即可确认。
